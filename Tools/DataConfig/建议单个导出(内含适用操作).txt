单个导出方式------>打开bat_out文件夹，选择<改动的excel文件名.bat>文件，双击运行即可。

使用范围：单个或多个表的改动操作，例如增加，修改，删除一个表的行列内容。

不适用范围：增加，删除、重命名excel文件，在excel文件中新增，删除，重命名（非#为前缀）表格。

作用：

1、仅会导出对应excel文件中的表格内容（C#的class文件，表格内容的二进制文件），极少的导出时间。
2、避免因导出没修改过的表，而引发潜在的问题（有编译错误，表格配置信息过时等）。